# CSS Structure - Структура стилей

## 📁 Модульная организация

Проект использует модульную структуру CSS для лучшей читаемости и поддержки.

### Структура файлов

```
public/css/
├── main.css              # Главный файл с @import
└── modules/              # Модули CSS
    ├── variables.css     # CSS переменные
    ├── base.css          # Базовые стили
    ├── header.css        # Шапка сайта
    ├── footer.css        # Подвал сайта
    ├── buttons.css       # Кнопки
    ├── cards.css         # Карточки продуктов
    ├── index.css         # Главная страница
    ├── about.css         # Страница "О компании"
    ├── catalog.css       # Страница каталога
    ├── animations.css    # Анимации
    └── responsive.css    # Адаптивность
```

## 🎨 CSS Переменные

В `variables.css` определены все основные переменные:

```css
--primary-color: #45befb;
--primary-dark: #006bbd;
--gradient-primary: linear-gradient(135deg, #45befb, #006bbd);
--text-primary: #1a1a1a;
--bg-white: #fff;
--border-radius: 8px;
--transition-normal: 0.3s;
```

### Использование переменных

```css
.my-element {
  color: var(--primary-color);
  background: var(--gradient-primary);
  border-radius: var(--border-radius);
  transition: all var(--transition-normal);
}
```

## 📝 Порядок импортов в main.css

1. **Variables** - CSS переменные (цвета, размеры, тени)
2. **Base** - Базовые стили (body, main)
3. **Layout** - Шапка и подвал
4. **Components** - Переиспользуемые компоненты
5. **Pages** - Стили для конкретных страниц
6. **Animations** - @keyframes анимации
7. **Responsive** - Media queries для адаптивности

## 🔧 Как добавить новый модуль

1. Создайте файл в `modules/`, например `modules/contacts.css`
2. Добавьте импорт в `main.css`:
   ```css
   @import url("modules/contacts.css");
   ```
3. Используйте CSS переменные вместо хардкода цветов

## ✅ Преимущества модульной структуры

- **Читаемость** - каждый файл отвечает за свою область
- **Поддержка** - легко найти и изменить нужный стиль
- **Переиспользование** - компоненты можно использовать на разных страницах
- **Командная работа** - несколько разработчиков могут работать одновременно
- **Масштабируемость** - легко добавлять новые модули

## 🎯 Best Practices

1. **Используйте CSS переменные** для цветов и размеров
2. **Избегайте дублирования** - выносите повторяющиеся стили в компоненты
3. **Группируйте** связанные стили в одном модуле
4. **Комментируйте** сложные селекторы
5. **Именуйте классы** по БЭМ или семантически

## 🔄 Миграция со старого main.css

Старый монолитный `main.css` сохранён для обратной совместимости, но новый код следует писать в модулях.

### Этапы миграции:

1. ✅ Создана структура модулей
2. ✅ Вынесены переменные
3. ✅ Разделены компоненты и страницы
4. ⏳ Постепенное удаление legacy кода

## 📚 Дополнительные материалы

- [CSS Variables (MDN)](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties)
- [CSS @import (MDN)](https://developer.mozilla.org/en-US/docs/Web/CSS/@import)
- [BEM Methodology](https://en.bem.info/methodology/)
